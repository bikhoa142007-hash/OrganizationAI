# Legacy source evidence

Legacy Role1 = BA; Legacy Role3 = Frontend Developer.

This directory retains imported versioned documents and archives under their original
names. Original wording, schema/model identifiers and archive bytes are provenance,
not current team labels. Only navigation links to relocated active fixtures were
updated in v2.1 Markdown; hashes are recorded in
[the import manifest](../integration/ba-import-manifest.json).
Active fixtures are now in tests/fixtures/ba/v2.1; adapter code is src/verify/ba_adapter.py.
